{
  "identifier": "default",
  "description": "Default capability for 900Notes",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-destroy",
    "dialog:default",
    "fs:default"
  ]
}
